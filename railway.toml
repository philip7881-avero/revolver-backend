[build]
builder = "NIXPACKS"

[deploy]
startCommand = "uvicorn revolver_v2:app --host 0.0.0.0 --port $PORT"
healthcheckPath = "/api/health"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
